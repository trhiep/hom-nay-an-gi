@page
@model HomNayAnGiApp.Pages.RecipeIngredientManage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RecipeIngredient[0].Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeIngredient[0].Unit)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeIngredient[0].Ingredient)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeIngredient[0].Recipe)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RecipeIngredient) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Unit)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ingredient.IngredientId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Recipe.RecipeId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.RecipeIngredientId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.RecipeIngredientId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.RecipeIngredientId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
